enum AppRegions {
  en_us,
}

enum AppLanguages {
  english,
}

enum AppCalendarTypes {
  georgian,
}

enum AppStorageKeys {
  keyContacts,
  keyAccountRecords,
  keySettings,
}

enum AppBottomNavigationPages {
  homepage,
  contacts,
  accounts,
  settings,
}

enum AppAccountsTableFilterType {
  byContact,
  byAmount,
  byDate,
}